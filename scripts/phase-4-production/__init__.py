"""
Phase 4: Production-Ready AI Systems

The shared PostgreSQL/pgvector models used by module 4.2 live in `utils/phase4_db.py`,
not here. This directory name contains hyphens, so it can never be imported as a Python
package — anything defined in this file would be unreachable.

    from utils.phase4_db import DATABASE_URL, Document, VectorDocument
"""
